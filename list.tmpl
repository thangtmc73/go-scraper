<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Page {{.Page}}</title>
    <link rel="stylesheet" src="./styles.css">
</head>
<body>
    <h1>List</h1>
    <div class="list">
        {{range .List }}
            <div>
                <img src="{{.Image}}" alt="{{.Name}}"/>
                <p>{{.Name}}</p>
            </div>
        {{end}}
    </div>
</body>